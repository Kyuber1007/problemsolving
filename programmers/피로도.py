def solution(k, dungeons):
    answer = -1
    length = len(dungeons)
    for i in range(length):
        stack = [i]
        visited = []
        while len(visited) < length:
            stack.pop()       
        
    
  
solution(80, [[80,20],[50,40],[30,10]])